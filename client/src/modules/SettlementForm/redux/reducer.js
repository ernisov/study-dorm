const INITIAL_STATE = {
  rooms: [],
  loading: false,
  dormitory: undefined,
  floor: undefined
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
